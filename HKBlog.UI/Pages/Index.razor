@page "/"
@inject NavigationManager navManager
@inject IStore store
@implements IDisposable

<ProductView/>

@code {
    protected override void OnInitialized()
    {
        store.AddStateChangedListeners(() => { StateHasChanged(); });
    }
    public void Dispose()
    {
        store.RemoveStateChangedListeners(() => { StateHasChanged(); });
    }

     /*
     *work on search such that it will go to home from anywhere products
     are searched.
     Work on this table counts that initializes from zero
     * On adds clicked, track and keep record
    */
}



